<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <Button
        android:id="@+id/show_toast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:text="To show toast in boardcast" />

    <Button
        android:id="@+id/show_dialog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/show_toast"
        android:layout_below="@+id/show_toast"
        android:layout_marginTop="30dp"
        android:text="To show dialog in boardcast" />

    <Button
        android:id="@+id/show_dialog_with_transparent_activity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/show_dialog"
        android:layout_below="@+id/show_dialog"
        android:layout_centerVertical="true"
        android:layout_marginTop="30dp"
        android:text="To show dialog with transparent activity" />

    <Button
        android:id="@+id/set_wallpaper"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/show_dialog_with_transparent_activity"
        android:layout_below="@+id/show_dialog_with_transparent_activity"
        android:layout_marginTop="30dp"
        android:text="Send am boardcast to set wallpaper" />

</RelativeLayout>